发布
    deployed xxxFactory to deploy instance(challenge)

问题
    Contract creation initialization returns data with length of more than 24576 bytes. The deployment will likely fails.
    More info: eip-170
    开启编译优化即可
